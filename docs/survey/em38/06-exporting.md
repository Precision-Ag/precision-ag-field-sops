---
title: Exporting
---
# :material-database-export: Exporting

Get the completed EM38 survey off the Getac and confirm it has reached the GIS team.
This follows on from [Survey Setup](05-survey-setup.md).

!!! warning "DRAFT: content pending the Subsoil software SOP"
    Export steps depend on Subsoil's export flow, which isn't confirmed yet. The DualEM
    export process (T3RRA, CSV, `PA Survey – General – Deal ID`) is included below as a
    likely-similar reference — do not treat it as confirmed for EM38/Subsoil.

!!! info "AT A GLANCE"
    `[CONFIRM: Subsoil export format and destination folder for EM38 data.]`

## 1. Export the data

`[CONFIRM: Subsoil export steps — format (CSV/shapefile), what to select, where it saves.]`

## 2. Confirm and send to SharePoint

`[CONFIRM: whether Subsoil uploads to SharePoint automatically like T3RRA did, and the
working folder naming convention.]`

!!! warning "WARNING"
    Do not leave the field assuming the upload worked if you never saw a confirmation.
    If the export did not land where expected, see [Troubleshooting](07-troubleshooting.md).
